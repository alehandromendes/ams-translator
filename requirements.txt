# AMS Translator — app de desktop (Windows)
PySide6>=6.6
mss>=9.0
Pillow>=10.0
numpy>=1.24
rapidocr-onnxruntime>=1.2
requests>=2.31
certifi>=2024.2
keyboard>=0.13

# só para gerar o .exe (build_exe.bat / overlay.spec)
pyinstaller>=6.10
