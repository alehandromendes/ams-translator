# python
.venv/
__pycache__/
*.pyc

# dados do usuário / runtime
overlay_config.json
data/overlay_shots/
/gamefill/
/traducoes/

# cache de tradução — versionar (trabalho acumulado)
!data/
!data/cache/
!data/cache/overlay_tm.json

# build (PyInstaller / Inno Setup)
build/
dist/
dist_new/
*.spec.bak
installer/Output/

# o instalador publicado fica versionado em download/ via Git LFS
!download/
!download/*.exe

# editores
.vscode/
.idea/

# traducao PT pre-construida (37MB, gerada por gamepatch build; vai no instalador/Release)
overlay/gamefill/prebuilt/
